<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Общий секрет с процессом mediasoup (Node)
    |--------------------------------------------------------------------------
    |
    | Если задан, запросы join / leave / close к API должны содержать заголовок
    | X-Mediasoup-Secret с тем же значением (его добавляет только mediasoup-server).
    |
    */

    'internal_secret' => env('MEDIASOUP_INTERNAL_SECRET'),

];
